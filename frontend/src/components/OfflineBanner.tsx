export function OfflineBanner() {
  return (
    <div className="sticky top-0 z-50 bg-pos-warning px-4 py-1.5 text-center text-xs font-medium text-black">
      Sin conexión — los datos se sincronizarán cuando vuelva la red
    </div>
  );
}
