{
  "name": "pos-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx wrangler pages deploy dist --project-name=pos-system-frontend"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "idb": "^8.0.0",
    "preact": "^10.26.0",
    "tailwindcss": "^4.3.0",
    "wouter-preact": "^3.0.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0",
    "wrangler": "^4.0.0"
  }
}
